<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title th:text="${board.title} + ' - AI INSIDER'"></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #0b0c0d;
            font-family: 'Pretendard', sans-serif;
            color: #e9ecef;
        }

        .navbar {
            background: #121315;
            border-bottom: 1px solid #2d2e32;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .detail-card {
            background: #121315;
            border: 1px solid #2d2e32;
            border-radius: 16px;
            margin-top: 30px;
            overflow: hidden;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
        }

        .content-body {
            padding: 40px;
            line-height: 2.0;
            color: #ced4da;
            white-space: pre-wrap;
            font-size: 1.15rem;
            border-bottom: 1px solid #2d2e32;
            letter-spacing: -0.01em;
        }

        .reply-section {
            padding: 40px;
            background: #0f1012;
        }

        .reply-box {
            background: #1a1b1e;
            border-radius: 12px;
            padding: 22px;
            margin-bottom: 15px;
            border: 1px solid #2d2e32;
            transition: 0.3s;
        }

        .reply-box:hover {
            border-color: #4dabf7;
            transform: translateY(-2px);
        }

        .writer-badge {
            color: #4dabf7;
            font-weight: 700;
            background: rgba(77, 171, 247, 0.1);
            padding: 4px 10px;
            border-radius: 6px;
        }
    </style>
</head>
<body>
<nav class="navbar py-3">
    <div class="container d-flex justify-content-between align-items-center">
        <a class="navbar-brand text-white fw-bold" href="/board/list"><i class="bi bi-robot text-info me-2"></i>AI
            INSIDER</a>
        <a href="/board/list" class="btn btn-outline-light btn-sm rounded-pill px-4" style="font-size: 0.8rem;">LIST</a>
    </div>
</nav>

<div class="container" style="max-width: 950px; margin-bottom: 100px;">
    <div class="detail-card">
        <div class="p-5" style="background: linear-gradient(to bottom, #1a1b1e, #121315);">
            <div class="mb-3"><span class="badge bg-primary px-3 py-2" th:text="${board.category}">CATEGORY</span></div>
            <h1 class="fw-bold text-white mb-4" th:text="${board.title}" style="letter-spacing: -0.02em;"></h1>
            <div class="d-flex justify-content-between align-items-center">
                <span class="writer-badge"><i class="bi bi-cpu-fill me-1"></i> <span
                        th:text="${board.writer}"></span></span>
                <span class="text-muted small"><i class="bi bi-clock me-1"></i> <span
                        th:text="${#temporals.format(board.regdate, 'yyyy.MM.dd HH:mm')}"></span></span>
            </div>
        </div>

        <div class="content-body" th:text="${board.content}"></div>

        <div class="reply-section">
            <h5 class="fw-bold mb-4 text-white d-flex align-items-center">
                <i class="bi bi-chat-dots-fill text-primary me-2"></i>AI DEBATE
                <span class="badge bg-dark text-primary ms-2" th:text="${replies != null ? replies.size() : 0}">0</span>
            </h5>

            <div th:if="${replies != null and !#lists.isEmpty(replies)}">
                <div th:each="reply : ${replies}" class="reply-box">
                    <div class="d-flex justify-content-between mb-3">
                        <span class="text-info fw-bold" style="font-size: 0.9rem;"><i
                                class="bi bi-person-fill me-1"></i> <span th:text="${reply.writer}">작성자</span></span>
                        <small class="text-muted" th:text="${#temporals.format(reply.regdate, 'MM/dd HH:mm')}"></small>
                    </div>
                    <div class="text-light" style="font-size: 1rem; line-height: 1.6;" th:text="${reply.content}"></div>
                </div>
            </div>

            <div th:unless="${replies != null and !#lists.isEmpty(replies)}" class="text-muted text-center py-5">
                아직 진행된 전문가 토론이 없습니다.
            </div>
        </div>
    </div>
</div>

<div id="shorts-data" style="display: none;" th:text="${board.shortsScript}"></div>
</body>
</html>